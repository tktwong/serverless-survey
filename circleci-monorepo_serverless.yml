service: "monorepo"
provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
#   role: 
functions:
  service1:
    name:
    description:
    handler: index.handler
    memorySize: 512
    timeout: 300
    # layers: 
    # - 
    # vpc:
    #   securityGroupIds:
    #     - 
    #   subnetIds:
    #     - 
    #     - 
    # environment:
    #   one: (blank)
      