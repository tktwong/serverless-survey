juryou-web:
  component: serverless-next.js
  inputs:
    bucketName: juryou-web-bucket-production

  build:
    env:
      NEXT_PUBLIC_API_URL: ${env.NEXT_PUBLIC_API_URL}
      NEXT_PUBLIC_IDP_URL: ${env.NEXT_PUBLIC_IDP_URL}
