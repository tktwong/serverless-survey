
service: technical-meeting-botlex

provider:
  name: aws
  runtime: nodejs12.x
  environment:
    ZAMMAD_HOST: https://backend-inlira.sandbox.stefanini.io/
    ZAMMAD_TOKEN: TOKEN

functions:
  main:
    handler: app/handler.main
