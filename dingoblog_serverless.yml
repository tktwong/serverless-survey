org: dingobar
app: dingoblog
name: dingoblog
component: website

provider:
  name: aws
  stage: prod
  region: eu-north-1
  profile: home

inputs:
  src:
    src: ./src
    hook: npm run build
    dist: ./dist
