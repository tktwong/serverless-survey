
typeORM:
  name: ${param:StackTagName}-typeORM-${self:provider.stage}
  path: ./services/layers/src/typeORM/
apollo:
  name: ${param:StackTagName}-apollo-${self:provider.stage}
  path: ./services/layers/src/apollo
