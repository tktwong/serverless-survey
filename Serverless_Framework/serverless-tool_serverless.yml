#组件信息
component: layer # (必填) 组件名称，此处为 nuxtjs
name: nuxtjs-tool-layer # (必填) 实例名称

inputs:
  region: ${env:REGION}
  name: ${name}
  src: 
    src: ../node_modules
    targetDir: /node_modules
  runtimes:
    - Nodejs10.15
    - Nodejs12.16