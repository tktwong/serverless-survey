org: dahorder
app: dah-order
name: adminpanel2
component: website
stage: prod

inputs:
  src:
    src: ./src
    hook: npm run build
    dist: ./dist
  region: ap-southeast-1
  bucketName: ${stage}-admin.dahorder.my
  domain: ${stage}-admin.dahorder.my