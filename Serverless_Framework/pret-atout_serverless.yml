service: ludo-graphql-client

frameworkVersion: ">=1.21.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs6.10
  stage: dev
  region: eu-west-1

plugins:
  - serverless-finch

custom:
  client:
    bucketName: pret-atout
    distributionFolder: build